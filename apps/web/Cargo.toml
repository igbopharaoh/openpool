[package]
name = "openpool-web-app"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
dioxus.workspace = true
openpool-web = { path = "../../crates/openpool-web" }

[features]
default = ["ssr"]
ssr = []
